interface:
  display_name: "Sync CI Protection Checks"
  short_description: "Align protection with the current CI execution path"
  default_prompt: "Use $sync-ci-protection-checks to reconcile required checks with the workflow checks generated for the protected event."
