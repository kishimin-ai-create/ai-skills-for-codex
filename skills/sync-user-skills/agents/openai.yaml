interface:
  display_name: "Sync User Skills"
  short_description: "Preserve the newer copy of every shared Skill"
  default_prompt: "Use $sync-user-skills to reconcile $HOME/.agents/skills and $HOME/.claude/skills from whichever side advanced."
