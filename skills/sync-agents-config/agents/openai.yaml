interface:
  display_name: "AGENTS.md Sync"
  short_description: "Preserve the newer shared AGENTS.md content"
  default_prompt: "Use $sync-agents-config to reconcile the repository AGENTS.md with $HOME/.codex/AGENTS.md from whichever side advanced."
